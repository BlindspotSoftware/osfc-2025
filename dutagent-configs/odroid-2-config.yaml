version: 0
devices:
  odroid:
    desc: "Odroid-H4-Board"
    cmds:
      flash:
        desc: "Flash operations for the Odroid-H4-board"
        modules:
          - module: flash
            main: true
            options:
              tool: "/usr/sbin/flashrom"
              programmer: "dediprog"

      power:
        desc: "Control the power via WiFi socket"
        modules:
          - module: wifisocket
            main: true
            options:
              host: http://192.168.8.62
              channel: 1

      serial:
        desc: "Serial console for the Odroid-H4-board"
        modules:
          - module: serial
            main: true
            options:
              port: /dev/serial/by-id/usb-9elements_Picoprog_OSFC2024-if02
              baudrate: 115200
